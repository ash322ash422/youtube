1) Create main.py file.

2) Now we build the image:
..\docker_tutorial\dock_tut1> docker build -t python-soup .

NOTE: Whenever we change main.py file, we have to rebuild the image.
TO remove image: docker rmi -f <image_id>

3) Run the image:
..\docker_tutorial\dock_tut1> docker run python-soup
<title>Quality Tutorials, Video Courses, and eBooks</title>
##############
link= /index.htm